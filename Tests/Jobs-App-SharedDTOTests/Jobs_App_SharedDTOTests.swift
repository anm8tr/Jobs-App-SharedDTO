import XCTest
@testable import Jobs_App_SharedDTO

final class Jobs_App_SharedDTOTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Jobs_App_SharedDTO().text, "Hello, World!")
    }
}
